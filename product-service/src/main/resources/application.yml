spring:
  application:
    name: product-service
  config:
    import: "optional:configserver:http://localhost:8888"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  data:
    rest:
      base-path: /api
